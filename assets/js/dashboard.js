document.addEventListener("DOMContentLoaded", () => {
    setupImagePreview(
        "fileInput",
        "previewImage",
        "fileName"
    );
    setupImagePreview(
        "studentFileInput",
        "studentPreview",
        "studentFileName"
    );
});